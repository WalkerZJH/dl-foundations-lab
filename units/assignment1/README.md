# Assignment 1

本单元对应 CS231n Assignment 1。

主要内容包括 kNN、Softmax、TwoLayerNet、FullyConnectedNet、优化器和向量化实现。

## 目录说明

* `code/`：放置 assignment1 相关代码。
* `notes/`：放置 assignment1 学习笔记。
* `results/`：放置可轻量保存的实验结果截图、表格或文本记录。

## 数据说明

原始 CIFAR-10 数据集不上传到 GitHub。如需复现实验，请在本地按课程说明下载数据，并保持数据文件位于被 `.gitignore` 忽略的位置。

## 当前实现状态

当前已补全 kNN、Softmax、SVM、LinearClassifier、TwoLayerNet、FullyConnectedNet、常用网络层和优化器，并提供统一实验脚本 `code/run_assignment1_experiments.py`。

实验结果集中保存在 `results/`，项目结构和任务实现规划保存在 `notes/`。
